# 🤖 LSTM Trading Assistant - URL Rewrite Rules
# Автоматично згенеровано Claude Code

# Увімкнути rewrite engine
RewriteEngine On

# API маршрути
RewriteRule ^api/analyze$ index.php?action=analyze [QSA,L]
RewriteRule ^health$ index.php?action=health [L]

# Головна сторінка
DirectoryIndex index.php

# Безпека - заборонити доступ до Python файлів
<Files "*.py">
    Order deny,allow
    Deny from all
</Files>

# Заборонити доступ до конфігураційних файлів
<Files ~ "^\.">
    Order deny,allow
    Deny from all
</Files>

# MIME типи для JSON
AddType application/json .json

# Налаштування для Python CGI (якщо потрібно)
AddHandler cgi-script .py
Options +ExecCGI

# Кешування статичних файлів
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
</IfModule>

# Стиснення
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>